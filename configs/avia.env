# VoxelMap (ROS1 / Noetic) config for MandEye datasets

INPUT_LIDAR_TOPIC=/livox/lidar
INPUT_IMU_TOPIC=/livox/imu

OUTPUT_DIR=results-voxelmap

RECORD_TOPICS="/cloud_registered /aft_mapped_to_init"

VOXELMAP_LAUNCH="voxel_map avia.launch"

ROSPLAY_ARGS="--clock"

